buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      build:
        preSteps:
        - name: inject-version
          sh: make skaffold.yaml.new && mv skaffold.yaml.new skaffold.yaml
    release:
      build:
        preSteps:
        - name: inject-version
          sh: make skaffold.yaml.new && mv skaffold.yaml.new skaffold.yaml
